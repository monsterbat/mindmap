[project]
name = "mindmap"
version = "0.1.0"
description = "A mind map a human and an AI edit together"
readme = "README.md"
# 開發環境 3.10+;⚠️ 執行環境是 /usr/bin/python3(3.9),main.py 語法必須相容 3.9(make check 會驗)
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
